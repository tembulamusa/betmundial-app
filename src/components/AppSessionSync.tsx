import React from "react";

import { useAppSessionSync } from "../hooks/useAppSessionSync";

const AppSessionSync = () => {
    useAppSessionSync();
    return null;
};

export default AppSessionSync;
